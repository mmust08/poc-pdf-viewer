# Claude Code version installed into the devcontainer image.
# Use "latest" to always pull the newest release, or pin to a specific
# version (e.g. CLAUDE_CODE_VERSION=1.2.3) to keep builds reproducible.
# Changes here require a full "Rebuild Container" to take effect.
CLAUDE_CODE_VERSION=latest
